---
title: "Visualization of Zhang Group Data"
output: flexdashboard::flex_dashboard
runtime: shiny
output: html_document
    
# pango cairo 
---

```{r global, include=FALSE}
#library(rsconnect)
#deployApp('../demoApp')
library(shiny)
```

hello

```{r, echo=FALSE}
#runApp("../interactwebinves/shinyapps001/", launch.browser = TRUE)
library(shiny)
library(nasaweather)
#source('./storm_helpers.R')

#selectInput("none","Choose a storm:",
#            choices = unique(storms$name),
#            selected = storms$name[1])d
data <- reactive(subset(storms, name == input$name))
```

```{r, echo=FALSE}
renderPlot(storm_plot(data()))
```